# WARNING: DO NOT EDIT!
#
# This file was generated by plugin_template, and is managed by it. Please use
# './plugin-template --github pulp_python' to update this file.
#
# For more info visit https://github.com/pulp/plugin_template

---
name: "Python Bleeding Edge CI"
on:
  workflow_dispatch:
    inputs:
      pulpcore_ref:
        description: >
          pulpcore ref to test against: branch name, commit SHA, or PR number.

          PR numbers are automatically converted to refs/pull/<number>/head.
        required: true
        type: "string"

concurrency:
  group: "${{ github.ref_name }}-${{ github.workflow }}"
  cancel-in-progress: true

jobs:
  build:
    uses: "./.github/workflows/build.yml"
    with:
      pulpcore_ref: "${{ inputs.pulpcore_ref }}"

  test:
    needs: "build"
    uses: "./.github/workflows/test.yml"
    with:
      matrix_env: |
        [{"TEST": "pulp"}, {"TEST": "azure"}, {"TEST": "s3"}]
      pulpcore_ref: "${{ inputs.pulpcore_ref }}"
...
